# 単なる足し算．
x = 1 + 2

# [Int] で，Int 値を 1 つ受け取ってそのまま返す関数になる．
y = [Int]
# つまり例えば 2 を渡せば 2 が返ってくる
x = y(2)

# さて，x と y をかける．
y = x * y
# x は Int だが，一方 y は Int を受け取って Int を返す関数．
# つまり，関数 y を定数 x 倍している．
# -> 引数を 2 倍して返す関数になる．
y(7)
# 7 が 2 倍されて 14 になる．

# 高度な使い方：
z = y + y([Int] + 1)
z(10)
